import { useEffect, useState } from 'react'
import { supabase } from '../lib/supabase'
import { useT } from '../lib/i18n'
import type { Accommodation, HostCompany, InsuranceProvider, LanguageCourseProvider, Participant, SendingOrganisation, TransferProvider } from '../types'
import ConfirmDialog from '../components/ConfirmDialog'
interface ParticipantsPageProps { typology: 'Incoming' | 'Outgoing'; groupView?: boolean }
type ViewMode = 'list' | 'grid'
function Badge({ text, color }: { text: string; color: string }) { return <span style={{ display: 'inline-block', padding: '2px 8px', borderRadius: 12, fontSize: 11, fontWeight: 600, background: `${color}22`, color, border: `1px solid ${color}44` }}>{text}</span> }
function FR({ label, name, value, editing, onChange, empty, type = 'text', options }: { label: string; name: string; value: string | number | null; editing: boolean; onChange: (name: string, val: string) => void; empty: string; type?: string; options?: string[] }) { return <div className="field-row"><div className="field-label">{label}</div>{editing ? (options ? <select className="form-input form-input-sm" value={value ?? ''} onChange={e => onChange(name, e.target.value)}><option value="">{empty}</option>{options.map(o => <option key={o} value={o}>{o}</option>)}</select> : <input className="form-input form-input-sm" type={type} value={value ?? ''} onChange={e => onChange(name, e.target.value)} />) : <div className="field-value">{value || empty}</div>}</div> }
function FRRef({ label, name, valueId, valueName, editing, onChange, options, empty }: { label: string; name: string; valueId: string | null; valueName: string | null; editing: boolean; onChange: (name: string, val: string) => void; options: { id: string; name: string }[]; empty: string }) { return <div className="field-row"><div className="field-label">{label}</div>{editing ? <select className="form-input form-input-sm" value={valueId ?? ''} onChange={e => onChange(name, e.target.value)}><option value="">{empty}</option>{options.map(o => <option key={o.id} value={o.id}>{o.name}</option>)}</select> : <div className="field-value">{valueName || empty}</div>}</div> }
const EMPTY_PARTICIPANT: Partial<Participant> = { name: '', surname: '', sex: '', status: '', nationality: '', marital_status: '', mobility_typology: '', indiv_group: 'Individual', email: '', phone: '', mobile_phone: '', address: '', postcode: '', city: '', country: '', destination_country: '', destination_city: '', program: '', group_name: '', project_name: '', grant_amount: null, lang_english: '', lang_spanish: '', lang_french: '', lang_german: '', lang_italian: '' }
export default function ParticipantsPage({ typology, groupView = false }: ParticipantsPageProps) {
  const { t } = useT()
  const [participants, setParticipants] = useState<Participant[]>([])
  const [selected, setSelected] = useState<Participant | null>(null)
  const [search, setSearch] = useState(''); const [loading, setLoading] = useState(true); const [editing, setEditing] = useState(false); const [editData, setEditData] = useState<Partial<Participant>>({}); const [saving, setSaving] = useState(false); const [showConfirm, setShowConfirm] = useState(false); const [isNew, setIsNew] = useState(false); const [saveError, setSaveError] = useState(''); const [viewMode, setViewMode] = useState<ViewMode>(() => localStorage.getItem('managemob_individual_view') === 'grid' ? 'grid' : 'list')
  const [sendingOrgs, setSendingOrgs] = useState<SendingOrganisation[]>([]); const [hostCompanies, setHostCompanies] = useState<HostCompany[]>([]); const [insuranceProviders, setInsuranceProviders] = useState<InsuranceProvider[]>([]); const [transferProviders, setTransferProviders] = useState<TransferProvider[]>([]); const [langCourseProviders, setLangCourseProviders] = useState<LanguageCourseProvider[]>([]); const [accommodations, setAccommodations] = useState<Accommodation[]>([])
  useEffect(() => { loadParticipants(); loadRefs() }, [typology])
  const loadRefs = async () => { const [so, hc, ins, tr, lcp, acc] = await Promise.all([supabase.from('sending_organisations').select('*').order('name'), supabase.from('host_companies').select('*').order('name'), supabase.from('insurance_providers').select('*').order('name'), supabase.from('transfer_providers').select('*').order('name'), supabase.from('language_course_providers').select('*').order('name'), supabase.from('accommodation').select('*').order('name')]); setSendingOrgs(so.data || []); setHostCompanies(hc.data || []); setInsuranceProviders(ins.data || []); setTransferProviders(tr.data || []); setLangCourseProviders(lcp.data || []); setAccommodations(acc.data || []) }
  const loadParticipants = async () => { setLoading(true); const { data, error } = await supabase.from('participants').select(`*, sending_organisations(id,name), host_companies(id,name,address,city,sector,tutor,tutor_phone,tutor_email), insurance_providers(id,name), transfer_providers(id,name), language_course_providers(id,name), accommodation_1:accommodation!participants_accommodation_1_id_fkey(id,name,typology,address,city,country), accommodation_2:accommodation!participants_accommodation_2_id_fkey(id,name,typology,address,city,country)`).eq('mobility_typology', typology).order('surname', { ascending: true }); if (error) console.error(error); else setParticipants((data as unknown as Participant[]) || []); setLoading(false) }
  const filtered = participants.filter(p => { const q = search.toLowerCase(); return `${p.name} ${p.surname}`.toLowerCase().includes(q) || (p.sending_organisations?.name || '').toLowerCase().includes(q) || (p.nationality || '').toLowerCase().includes(q) })
  const grouped: Record<string, Participant[]> = {}; filtered.forEach(p => { const org = p.sending_organisations?.name || t('list_no_org'); if (!grouped[org]) grouped[org] = []; grouped[org].push(p) })
  const selectParticipant = (p: Participant) => { setSelected(p); setEditing(false); setIsNew(false); setSaveError('') }
  const startEdit = () => { setEditData({ ...selected }); setEditing(true); setSaveError('') }
  const startNew = () => { setEditData({ ...EMPTY_PARTICIPANT, mobility_typology: typology }); setSelected(null); setIsNew(true); setEditing(true); setSaveError('') }
  const cancelEdit = () => { setEditing(false); setIsNew(false); setSaveError('') }
  const setParticipantView = (mode: ViewMode) => { setViewMode(mode); localStorage.setItem('managemob_individual_view', mode) }
  const handleChange = (name: string, val: string) => setEditData(prev => ({ ...prev, [name]: val === '' ? null : val }))
  const handleSave = async () => { setSaving(true); setSaveError(''); const payload = { ...editData } as any; delete payload.sending_organisations; delete payload.host_companies; delete payload.insurance_providers; delete payload.transfer_providers; delete payload.language_course_providers; delete payload.accommodation_1; delete payload.accommodation_2; if (isNew) { const { error } = await supabase.from('participants').insert({ ...payload, id: `rec${Date.now()}` }); if (error) { setSaveError(error.message); setSaving(false); return } } else { const { error } = await supabase.from('participants').update(payload).eq('id', selected!.id); if (error) { setSaveError(error.message); setSaving(false); return } } await loadParticipants(); setEditing(false); setIsNew(false); setSaving(false) }
  const handleDelete = async () => { if (!selected) return; await supabase.from('participants').delete().eq('id', selected.id); setSelected(null); setShowConfirm(false); await loadParticipants() }
  const pdfBase = 'https://pdf.managemob.app/pdf-viewer'; const profLevels = ['A1', 'A2', 'B1', 'B2', 'C1', 'C2']; const empty = t('none'); const refProps = { editing, onChange: handleChange, empty }; const fields = editing ? editData : (selected || {})
  return <div className="split-layout">{showConfirm && <ConfirmDialog message={`${t('part_delete_confirm')}: ${selected?.name} ${selected?.surname}? ${t('confirm_irrev')}`} onConfirm={handleDelete} onCancel={() => setShowConfirm(false)} />}<div className="split-left"><div className="split-header"><h2 className="split-title">{groupView ? t('page_groups') : t('page_individuals')} - {typology}</h2><span className="badge-count">{filtered.length}</span></div><div className="search-bar participant-toolbar"><input type="text" className="form-input" placeholder={t('part_search')} value={search} onChange={e => setSearch(e.target.value)} />{!groupView && <div className="view-toggle" aria-label="View mode"><button type="button" className={`view-toggle-btn ${viewMode === 'list' ? 'active' : ''}`} onClick={() => setParticipantView('list')} title="List view">≣</button><button type="button" className={`view-toggle-btn ${viewMode === 'grid' ? 'active' : ''}`} onClick={() => setParticipantView('grid')} title="Grid view">⊞</button></div>}<button className="btn btn-accent btn-sm participant-add-btn" onClick={startNew} title={t('part_new')}>{t('btn_new')}</button></div>{loading ? <div className="list-loading"><div className="spinner-sm"></div></div> : viewMode === 'grid' && !groupView ? <div className="participant-grid">{filtered.map(p => <button key={p.id} type="button" className={`participant-grid-card ${selected?.id === p.id ? 'selected' : ''}`} onClick={() => selectParticipant(p)}><div className="participant-grid-top"><div className="participant-avatar">{(p.name || '?').charAt(0)}{(p.surname || '').charAt(0)}</div><Badge text={p.indiv_group || 'Individual'} color={p.indiv_group === 'Group' ? '#8B5CF6' : '#2D7A6F'} /></div><div className="participant-grid-name">{p.name} {p.surname}</div><div className="participant-grid-org">{p.sending_organisations?.name || t('list_no_org')}</div><div className="participant-grid-meta">{[p.nationality, p.destination_city].filter(Boolean).join(' · ') || empty}</div></button>)}{filtered.length === 0 && <div className="empty-state">{t('list_empty')}</div>}</div> : <div className="participant-list">{Object.entries(grouped).map(([org, members]) => <div key={org} className="org-group"><div className="org-group-header"><span>{org}</span><span className="org-count">{members.length}</span></div>{members.map(p => <div key={p.id} className={`participant-item ${selected?.id === p.id ? 'selected' : ''}`} onClick={() => selectParticipant(p)}><div className="participant-avatar">{(p.name || '?').charAt(0)}{(p.surname || '').charAt(0)}</div><div className="participant-info"><div className="participant-name">{p.name} {p.surname}</div><div className="participant-meta">{[p.nationality, p.destination_city].filter(Boolean).join(' · ') || empty}</div></div><Badge text={p.indiv_group || 'Individual'} color={p.indiv_group === 'Group' ? '#8B5CF6' : '#2D7A6F'} /></div>)}</div>)}{filtered.length === 0 && <div className="empty-state">{t('list_empty')}</div>}</div>}</div><div className="split-right">{(selected || isNew) ? <div className="detail-panel">{!editing ? <div className="detail-action-bar"><div className="action-bar-left"><Badge text={selected?.mobility_typology || typology} color="#2D7A6F" /><Badge text={selected?.indiv_group || 'Individual'} color="#1D72B8" /></div><div className="action-bar-right">{selected && <><a href={`${pdfBase}/${selected.id}/participants`} target="_blank" rel="noopener noreferrer" className="btn btn-accent btn-sm">{t('pdf_voucher')}</a><a href={`${pdfBase}/${selected.id}/financial-report`} target="_blank" rel="noopener noreferrer" className="btn btn-primary btn-sm">{t('pdf_financial')}</a><a href={`${pdfBase}/${selected.id}/certificate`} target="_blank" rel="noopener noreferrer" className="btn btn-secondary btn-sm">{t('pdf_certificate')}</a></>}<button className="btn btn-edit btn-sm" onClick={startEdit}>{t('btn_edit')}</button>{selected && <button className="btn btn-danger btn-sm" onClick={() => setShowConfirm(true)}>{t('btn_delete')}</button>}</div></div> : <div className="detail-action-bar"><span style={{ fontWeight: 600, color: '#2D7A6F' }}>{isNew ? t('part_new') : t('detail_mode_edit')}</span><div className="action-bar-right">{saveError && <span className="save-error">{saveError}</span>}<button className="btn btn-secondary btn-sm" onClick={cancelEdit} disabled={saving}>{t('btn_cancel')}</button><button className="btn btn-accent btn-sm" onClick={handleSave} disabled={saving}>{saving ? t('btn_saving') : t('btn_save')}</button></div></div>}{!isNew && selected && <div className="detail-name-header"><div className="detail-avatar">{(selected.name || '?').charAt(0)}{(selected.surname || '').charAt(0)}</div><div><h2 className="detail-name">{selected.name} {selected.surname}</h2><p className="detail-id">{[selected.id_formattato, selected.record_number ? `Record #${selected.record_number}` : ''].filter(Boolean).join(' · ')}</p></div></div>}{isNew && <div className="detail-name-header"><div className="detail-avatar" style={{ background: '#1D72B8' }}>+</div><div><h2 className="detail-name">{t('part_new')}</h2></div></div>}<div className="detail-sections"><div className="detail-section-header">{t('sec_personal')}</div><div className="fields-grid"><FR label={t('fld_name')} name="name" value={fields.name ?? ''} {...refProps} /><FR label={t('fld_surname')} name="surname" value={fields.surname ?? ''} {...refProps} /><FR label={t('fld_sex')} name="sex" value={fields.sex ?? ''} options={['Male', 'Female', 'Other']} {...refProps} /><FR label={t('fld_status')} name="status" value={fields.status ?? ''} options={['Student', 'Teacher', 'Job seeker', 'Worker', 'Other']} {...refProps} /><FR label={t('fld_dob')} name="date_of_birth" value={fields.date_of_birth ?? ''} type="date" {...refProps} /><FR label={t('fld_pob')} name="place_of_birth" value={fields.place_of_birth ?? ''} {...refProps} /><FR label={t('fld_nationality')} name="nationality" value={fields.nationality ?? ''} {...refProps} /><FR label={t('fld_marital')} name="marital_status" value={fields.marital_status ?? ''} options={['Single', 'Married', 'Divorced', 'Widowed']} {...refProps} /><FR label={t('fld_driving')} name="driving_licence" value={fields.driving_licence ?? ''} options={['Yes', 'No']} {...refProps} /></div><div className="detail-section-header">{t('sec_address')}</div><div className="fields-grid"><FR label={t('fld_address')} name="address" value={fields.address ?? ''} {...refProps} /><FR label={t('fld_postcode')} name="postcode" value={fields.postcode ?? ''} {...refProps} /><FR label={t('fld_city')} name="city" value={fields.city ?? ''} {...refProps} /><FR label={t('fld_country')} name="country" value={fields.country ?? ''} {...refProps} /></div><div className="detail-section-header">{t('sec_contacts')}</div><div className="fields-grid"><FR label={t('fld_email')} name="email" value={fields.email ?? ''} type="email" {...refProps} /><FR label={t('fld_phone')} name="phone" value={fields.phone ?? ''} {...refProps} /><FR label={t('fld_mobile')} name="mobile_phone" value={fields.mobile_phone ?? ''} {...refProps} /></div><div className="detail-section-header">{t('sec_passport')}</div><div className="fields-grid"><FR label={t('fld_passport_no')} name="passport_number" value={fields.passport_number ?? ''} {...refProps} /><FR label={t('fld_passport_exp')} name="passport_expiring_date" value={fields.passport_expiring_date ?? ''} type="date" {...refProps} /></div><div className="detail-section-header">{t('sec_languages')}</div><div className="fields-grid"><FR label={t('fld_lang_english')} name="lang_english" value={fields.lang_english ?? ''} options={profLevels} {...refProps} /><FR label={t('fld_lang_spanish')} name="lang_spanish" value={fields.lang_spanish ?? ''} options={profLevels} {...refProps} /><FR label={t('fld_lang_french')} name="lang_french" value={fields.lang_french ?? ''} options={profLevels} {...refProps} /><FR label={t('fld_lang_german')} name="lang_german" value={fields.lang_german ?? ''} options={profLevels} {...refProps} /><FR label={t('fld_lang_italian')} name="lang_italian" value={fields.lang_italian ?? ''} options={profLevels} {...refProps} /><FR label={t('fld_lang_other')} name="lang_other" value={fields.lang_other ?? ''} {...refProps} /><FR label={t('fld_lang_test')} name="language_test_validated" value={fields.language_test_validated ?? ''} {...refProps} /><FR label={t('fld_lang_score')} name="language_test_score" value={fields.language_test_score ?? ''} {...refProps} /><FR label={t('fld_lang_cert')} name="language_test_certificate" value={fields.language_test_certificate ?? ''} {...refProps} /></div><div className="detail-section-header">{t('sec_education')}</div><div className="fields-grid"><FR label={t('fld_diploma')} name="last_diploma" value={fields.last_diploma ?? ''} {...refProps} /><FR label={t('fld_year')} name="year_obtained" value={fields.year_obtained ?? ''} {...refProps} /></div><div className="detail-section-header">{t('sec_banking')}</div><div className="fields-grid"><FR label={t('fld_swift')} name="swift_code" value={fields.swift_code ?? ''} {...refProps} /><FR label={t('fld_iban')} name="iban" value={fields.iban ?? ''} {...refProps} /></div><div className="detail-section-header">{t('sec_mobility')}</div><div className="fields-grid"><FR label={t('fld_typology')} name="mobility_typology" value={fields.mobility_typology ?? ''} options={['Incoming', 'Outgoing']} {...refProps} /><FR label={t('fld_indiv_group')} name="indiv_group" value={fields.indiv_group ?? ''} options={['Individual', 'Group']} {...refProps} /><FR label={t('fld_dest_country')} name="destination_country" value={fields.destination_country ?? ''} {...refProps} /><FR label={t('fld_dest_city')} name="destination_city" value={fields.destination_city ?? ''} {...refProps} /><FR label={t('fld_arrival')} name="arrival_date" value={fields.arrival_date ?? ''} type="date" {...refProps} /><FR label={t('fld_departure')} name="departure_date" value={fields.departure_date ?? ''} type="date" {...refProps} /><FR label={t('fld_program')} name="program" value={fields.program ?? ''} {...refProps} /><FR label={t('fld_project')} name="project_name" value={fields.project_name ?? ''} {...refProps} /><FR label={t('fld_group_name')} name="group_name" value={fields.group_name ?? ''} {...refProps} /></div><div className="detail-section-header">{t('sec_providers')}</div><div className="fields-grid"><FRRef label={t('fld_sending_org')} name="sending_organisation_id" valueId={fields.sending_organisation_id ?? null} valueName={selected?.sending_organisations?.name ?? null} options={sendingOrgs} {...refProps} /><FRRef label={t('fld_host_company')} name="host_company_id" valueId={fields.host_company_id ?? null} valueName={selected?.host_companies?.name ?? null} options={hostCompanies} {...refProps} /><FRRef label={t('fld_insurance_prov')} name="insurance_provider_id" valueId={fields.insurance_provider_id ?? null} valueName={selected?.insurance_providers?.name ?? null} options={insuranceProviders} {...refProps} /><FRRef label={t('fld_transfer_prov')} name="transfer_provider_id" valueId={fields.transfer_provider_id ?? null} valueName={selected?.transfer_providers?.name ?? null} options={transferProviders} {...refProps} /><FRRef label={t('fld_lc_provider')} name="language_course_provider_id" valueId={fields.language_course_provider_id ?? null} valueName={selected?.language_course_providers?.name ?? null} options={langCourseProviders} {...refProps} /></div><div className="detail-section-header">{t('sec_lang_course')}</div><div className="fields-grid"><FR label={t('fld_lc_start')} name="language_course_start_date" value={fields.language_course_start_date ?? ''} type="date" {...refProps} /><FR label={t('fld_lc_end')} name="language_course_end_date" value={fields.language_course_end_date ?? ''} type="date" {...refProps} /><FR label={t('fld_lc_weeks')} name="language_course_weeks" value={fields.language_course_weeks ?? ''} type="number" {...refProps} /></div><div className="detail-section-header">{t('sec_internship')}</div><div className="fields-grid"><FR label={t('fld_int_start')} name="internship_start_date" value={fields.internship_start_date ?? ''} type="date" {...refProps} /><FR label={t('fld_int_end')} name="internship_end_date" value={fields.internship_end_date ?? ''} type="date" {...refProps} /><FR label={t('fld_int_weeks')} name="internship_weeks" value={fields.internship_weeks ?? ''} type="number" {...refProps} /></div><div className="detail-section-header">{t('sec_accommodation')}</div><div className="fields-grid"><FRRef label={t('fld_acc1')} name="accommodation_1_id" valueId={fields.accommodation_1_id ?? null} valueName={selected?.accommodation_1?.name ?? null} options={accommodations} {...refProps} /><FR label={t('fld_acc1_start')} name="accommodation_1_start_date" value={fields.accommodation_1_start_date ?? ''} type="date" {...refProps} /><FR label={t('fld_acc1_end')} name="accommodation_1_end_date" value={fields.accommodation_1_end_date ?? ''} type="date" {...refProps} /><FR label={t('fld_acc1_weeks')} name="accommodation_1_weeks" value={fields.accommodation_1_weeks ?? ''} type="number" {...refProps} /><FR label={t('fld_acc1_cost')} name="accommodation_1_cost" value={fields.accommodation_1_cost ?? ''} type="number" {...refProps} /><FRRef label={t('fld_acc2')} name="accommodation_2_id" valueId={fields.accommodation_2_id ?? null} valueName={selected?.accommodation_2?.name ?? null} options={accommodations} {...refProps} /><FR label={t('fld_acc2_start')} name="accommodation_2_start_date" value={fields.accommodation_2_start_date ?? ''} type="date" {...refProps} /><FR label={t('fld_acc2_end')} name="accommodation_2_end_date" value={fields.accommodation_2_end_date ?? ''} type="date" {...refProps} /><FR label={t('fld_acc2_weeks')} name="accommodation_2_weeks" value={fields.accommodation_2_weeks ?? ''} type="number" {...refProps} /><FR label={t('fld_acc2_cost')} name="accommodation_2_cost" value={fields.accommodation_2_cost ?? ''} type="number" {...refProps} /></div><div className="detail-section-header">{t('sec_financials')}</div><div className="fields-grid"><FR label={t('fld_grant')} name="grant_amount" value={fields.grant_amount ?? ''} type="number" {...refProps} /><FR label={t('fld_intl_transport')} name="international_transport_cost" value={fields.international_transport_cost ?? ''} type="number" {...refProps} /><FR label={t('fld_local_transport')} name="local_transport_cost" value={fields.local_transport_cost ?? ''} type="number" {...refProps} /><FR label={t('fld_food')} name="food_allowance_cost" value={fields.food_allowance_cost ?? ''} type="number" {...refProps} /><FR label={t('fld_insurance_cost')} name="insurance_cost" value={fields.insurance_cost ?? ''} type="number" {...refProps} /><FR label={t('fld_transfer_cost')} name="transfer_cost" value={fields.transfer_cost ?? ''} type="number" {...refProps} /><FR label={t('fld_other_expenses')} name="other_expenses" value={fields.other_expenses ?? ''} type="number" {...refProps} /><FR label={t('fld_other_notes')} name="note_other_expenses" value={fields.note_other_expenses ?? ''} {...refProps} /></div>{!editing && selected?.host_companies && <><div className="detail-section-header">{t('sec_host_company_detail')}</div><div className="fields-grid"><div className="field-row"><div className="field-label">{t('fld_company_name')}</div><div className="field-value">{selected.host_companies.name}</div></div><div className="field-row"><div className="field-label">{t('fld_city')}</div><div className="field-value">{selected.host_companies.city || empty}</div></div><div className="field-row"><div className="field-label">{t('fld_sector')}</div><div className="field-value">{selected.host_companies.sector || empty}</div></div><div className="field-row"><div className="field-label">{t('fld_tutor')}</div><div className="field-value">{selected.host_companies.tutor || empty}</div></div><div className="field-row"><div className="field-label">{t('fld_tutor_phone')}</div><div className="field-value">{selected.host_companies.tutor_phone || empty}</div></div><div className="field-row"><div className="field-label">{t('fld_tutor_email')}</div><div className="field-value">{selected.host_companies.tutor_email || empty}</div></div></div></>}{!editing && selected && <div style={{ padding: '16px 0', display: 'flex', gap: 8, flexWrap: 'wrap' }}><a href={`${pdfBase}/${selected.id}/participants`} target="_blank" rel="noopener noreferrer" className="btn btn-accent btn-sm">{t('pdf_voucher')}</a><a href={`${pdfBase}/${selected.id}/financial-report`} target="_blank" rel="noopener noreferrer" className="btn btn-primary btn-sm">{t('pdf_financial')}</a><a href={`${pdfBase}/${selected.id}/certificate`} target="_blank" rel="noopener noreferrer" className="btn btn-secondary btn-sm">{t('pdf_certificate')}</a></div>}</div></div> : <div className="detail-empty"><div className="detail-empty-icon">👤</div><p>{t('part_hint')}</p><button className="btn btn-accent" onClick={startNew} style={{ marginTop: 12 }}>{t('part_new_btn')}</button></div>}</div></div>
}
