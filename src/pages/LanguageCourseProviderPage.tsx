import { useEffect, useState } from 'react'
import { supabase } from '../lib/supabase'
import { useT } from '../lib/i18n'
import type { LanguageCourseProvider } from '../types'
import ConfirmDialog from '../components/ConfirmDialog'

const EMPTY: Partial<LanguageCourseProvider> = { name: '', address: '', city: '', country: '', contact_person: '', email: '', phone: '', language_taught: '' }
function FR({ label, name, value, editing, onChange, empty, type = 'text' }: { label: string; name: string; value: string | null; editing: boolean; onChange: (n: string, v: string) => void; empty: string; type?: string }) { return <div className="field-row"><div className="field-label">{label}</div>{editing ? <input className="form-input form-input-sm" type={type} value={value ?? ''} onChange={e => onChange(name, e.target.value)} /> : <div className="field-value">{value || empty}</div>}</div> }

export default function LanguageCourseProviderPage() {
  const { t } = useT()
  const [items, setItems] = useState<LanguageCourseProvider[]>([]); const [selected, setSelected] = useState<LanguageCourseProvider | null>(null); const [search, setSearch] = useState(''); const [loading, setLoading] = useState(true); const [editing, setEditing] = useState(false); const [editData, setEditData] = useState<Partial<LanguageCourseProvider>>({}); const [saving, setSaving] = useState(false); const [showConfirm, setShowConfirm] = useState(false); const [isNew, setIsNew] = useState(false); const [saveError, setSaveError] = useState('')
  const load = async () => { setLoading(true); const { data } = await supabase.from('language_course_providers').select('*').order('name'); setItems(data || []); setLoading(false) }
  useEffect(() => { load() }, [])
  const filtered = items.filter(p => p.name.toLowerCase().includes(search.toLowerCase()) || (p.city || '').toLowerCase().includes(search.toLowerCase()) || (p.language_taught || '').toLowerCase().includes(search.toLowerCase()))
  const select = (p: LanguageCourseProvider) => { setSelected(p); setEditing(false); setIsNew(false); setSaveError('') }
  const startNew = () => { setEditData({ ...EMPTY }); setSelected(null); setIsNew(true); setEditing(true); setSaveError('') }
  const startEdit = () => { setEditData({ ...selected }); setEditing(true); setSaveError('') }
  const cancel = () => { setEditing(false); setIsNew(false); setSaveError('') }
  const handleChange = (n: string, v: string) => setEditData(p => ({ ...p, [n]: v === '' ? null : v }))
  const handleSave = async () => { setSaving(true); setSaveError(''); if (isNew) { const { error } = await supabase.from('language_course_providers').insert({ ...editData, id: `rec${Date.now()}` }); if (error) { setSaveError(error.message); setSaving(false); return } } else { const { error } = await supabase.from('language_course_providers').update(editData).eq('id', selected!.id); if (error) { setSaveError(error.message); setSaving(false); return } } await load(); setEditing(false); setIsNew(false); setSaving(false) }
  const handleDelete = async () => { if (!selected) return; await supabase.from('language_course_providers').delete().eq('id', selected.id); setSelected(null); setShowConfirm(false); await load() }
  const v = (f: keyof LanguageCourseProvider) => editing ? (editData[f] as string ?? '') : (selected?.[f] as string ?? '')
  return <div className="split-layout">{showConfirm && <ConfirmDialog message={`${t('confirm_delete')} "${selected?.name}"? ${t('confirm_irrev')}`} onConfirm={handleDelete} onCancel={() => setShowConfirm(false)} />}<div className="split-left"><div className="split-header"><h2 className="split-title">{t('nav_languageCourseProviders')}</h2><span className="badge-count">{filtered.length}</span></div><div className="search-bar" style={{ display: 'flex', gap: 6 }}><input type="text" className="form-input" placeholder={t('list_search')} value={search} onChange={e => setSearch(e.target.value)} style={{ flex: 1 }} /><button className="btn btn-accent btn-sm" onClick={startNew}>{t('btn_new')}</button></div>{loading ? <div className="list-loading"><div className="spinner-sm"></div></div> : <div className="participant-list">{filtered.map(p => <div key={p.id} className={`participant-item ${selected?.id === p.id ? 'selected' : ''}`} onClick={() => select(p)}><div className="participant-avatar" style={{ background: '#8B5CF622', color: '#7C3AED' }}>{p.name.charAt(0)}</div><div className="participant-info"><div className="participant-name">{p.name}</div><div className="participant-meta">{p.language_taught ? `${p.language_taught} · ` : ''}{p.city || t('none')}</div></div></div>)}{filtered.length === 0 && <div className="empty-state">{t('list_empty')}</div>}</div>}</div><div className="split-right">{(selected || isNew) ? <div className="detail-panel"><div className="detail-action-bar">{editing ? <><span style={{ fontWeight: 600, color: '#2D7A6F' }}>{isNew ? t('detail_mode_new') : t('detail_mode_edit')}</span><div className="action-bar-right">{saveError && <span className="save-error">{saveError}</span>}<button className="btn btn-secondary btn-sm" onClick={cancel}>{t('btn_cancel')}</button><button className="btn btn-accent btn-sm" onClick={handleSave} disabled={saving}>{saving ? t('btn_saving') : t('btn_save')}</button></div></> : <><span /><div className="action-bar-right"><button className="btn btn-edit btn-sm" onClick={startEdit}>{t('btn_edit')}</button><button className="btn btn-danger btn-sm" onClick={() => setShowConfirm(true)}>{t('btn_delete')}</button></div></>}</div>{!isNew && selected && <div className="detail-name-header"><div className="detail-avatar" style={{ background: '#8B5CF622', color: '#7C3AED' }}>{selected.name.charAt(0)}</div><div><h2 className="detail-name">{selected.name}</h2><p className="detail-id">{t('nav_languageCourseProviders')}{selected.language_taught ? ` · ${selected.language_taught}` : ''}</p></div></div>}{isNew && <div className="detail-name-header"><div className="detail-avatar" style={{ background: '#7C3AED' }}>+</div><div><h2 className="detail-name">{t('detail_mode_new')}</h2></div></div>}<div className="detail-sections"><div className="detail-section-header">{t('sec_so_details')}</div><div className="fields-grid"><FR label={t('fld_lc_provider')} name="name" value={v('name')} editing={editing} onChange={handleChange} empty={t('none')} /><FR label={t('sec_lang_course')} name="language_taught" value={v('language_taught')} editing={editing} onChange={handleChange} empty={t('none')} /><FR label={t('fld_contact')} name="contact_person" value={v('contact_person')} editing={editing} onChange={handleChange} empty={t('none')} /><FR label={t('fld_email')} name="email" value={v('email')} editing={editing} onChange={handleChange} type="email" empty={t('none')} /><FR label={t('fld_phone')} name="phone" value={v('phone')} editing={editing} onChange={handleChange} empty={t('none')} /></div><div className="detail-section-header">{t('sec_address')}</div><div className="fields-grid"><FR label={t('fld_address')} name="address" value={v('address')} editing={editing} onChange={handleChange} empty={t('none')} /><FR label={t('fld_city')} name="city" value={v('city')} editing={editing} onChange={handleChange} empty={t('none')} /><FR label={t('fld_country')} name="country" value={v('country')} editing={editing} onChange={handleChange} empty={t('none')} /></div></div></div> : <div className="detail-empty"><div className="detail-empty-icon">📚</div><p>{t('detail_select_hint')}</p><button className="btn btn-accent" onClick={startNew} style={{ marginTop: 12 }}>{t('btn_new')}</button></div>}</div></div>
}
